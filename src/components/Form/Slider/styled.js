import styledCmp from 'styled-components'  
 
import { styled } from '@mui/material/styles'; 
 
import Slider from '@mui/material/Slider';
  
export const MaterialSlider = styled(Slider)(({ theme }) => ({  
})); 