import { Meta } from '@storybook/addon-docs';
import { storybookStyles } from './styles'  

<Meta title="Web/Docs/Install" />

<style>
  { storybookStyles }
</style>

# Install dependencies in your project
 
<div className="subheading">On root folder in your application</div>

Install dependencies with `yarn`
```sh
    yarn install
```

Or install dependencies with `npm`
```sh
    npm install
```

<div className="tip-wrapper">
  <span className="tip">Tip</span>if you have some problem to install project dependencies, check if your node version are greater than 14
  <code>💡</code>
</div>

<div className="subheading">Next step</div>

<div className="link-list"> 
  <a className="link-item" href="?path=/docs/web-docs-run--page" > 
    <span>
      <strong>Running Project</strong>
      Run your project in development mode
    </span>
  </a>
</div>